export interface productItem {
    _id: string
    name: string
    brand: string
    price: number
    image: string
    category: string
    description: string
    quantity: number
    inStock: boolean
    createdAt: string
    updatedAt: string
}